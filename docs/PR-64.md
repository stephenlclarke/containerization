# Pull request 64: sync Apple updates before 0.14.1

## Change

Pull request [#64](https://github.com/stephenlclarke/containerization/pull/64)
merges the current Apple `containerization` main branch into the support fork.
It brings in Swift Crypto 4 compatibility, CIDR bounds corrections, and the
new cctl NBD block-mount path while preserving the fork's runtime extensions.

## Review boundary

The merge resolved no semantic conflicts. Package and runtime changes use the
upstream implementations unchanged; the fork-only graph remains intact.

## Validation

- 52 focused `TestCIDR` and `BlockArgumentTests` cases pass.
- `git diff --check` passes.
- Commit signature verification passes.
- Hosted checks and the exact-head Codex review are required before merge.

Related issue: [#63](https://github.com/stephenlclarke/containerization/issues/63).
