# fix(ext4): decode little-endian values from unaligned storage

<!-- markdownlint-disable MD013 -->

Pull request: [103](https://github.com/stephenlclarke/containerization/pull/103).

## Motivation

Resolve the image-reader crash described in [the issue record](ISSUE-ext4-unaligned-load.md), discovered by the native Compose sanitizer lane.

## Implementation

Use `loadUnaligned(as:)` in both existing endian branches. Preserve the public generic signature and existing byte-order behavior; no allocation or copy is added outside the standard library's unaligned-load implementation. Add deterministic scalar regression coverage at every byte offset for 16-, 32- and 64-bit values. The patch is generic and contains no Compose policy.

Source follow-up `a13f5d347ac0723714d68e6f7802019673d04d97` factors the existing switch into an internal byte-order overload. The public method still supplies the detected `Endian`; no public API, global state or branch semantics change. Tests exercise both branch choices for all scalar widths and offsets on the current host, in addition to the existing public-path regression. This is branch simulation, not evidence from big-endian hardware.

## Validation

The preceding `5c45823` Sonar analysis failed at 50% new-code coverage because only the little-endian load ran. No coverage exclusion or gate change was made. The follow-up's equivalent native Compose regression passes both methods and every parameterized offset/branch (`c2c8d9b4-863d-4d04-a69b-5c7b371785fd`, 42.792 seconds build/test elapsed). This pre-publication proof fetched exact local Git commit `a13f5d3` through a temporary local source URL, not a mutable checkout override; consumers must use the canonical Stephen GitHub URL before publication. Independent source/API/branch-math review found no actionable issue. Full fork tests and a new exact-head Sonar result remain required.

The unchanged dependency fails the equivalent focused consumer regression at Bazel invocation `421c2b24-261b-4133-b2a6-5d4f87b5bc2e`. With the exact source commit `ea5ff3b97bd1a12c4054f262ce9fbb7207e6f443`, the complete affected Compose runtime unit suite passes under TSan at `7040f032-1f97-4660-8ec0-237c8e701dd8`: 32 tests in seven suites, including the offset regression and the original empty-volume/default-journal failure (13.274 seconds build/test elapsed). The full enhanced Compose TSan aggregate passes all eleven targets at `87d7bbd6-8aa2-4c20-8920-9aeb5230965b`, with two executed and nine cached targets (9.826 seconds). These are development timings, not quiet benchmarks.

Independent source/API/test review found no actionable issue. Focused Swift formatting/lint and Markdown checks pass. The regression is also included in the fork's native test directory; the actual executable proof above uses its equivalent consumer test, not a claim that the entire Containerization package suite ran. No SwiftPM fallback, retry, sanitizer suppression or upstream stock patch is used. Exact-head GitHub review/checks remain required before merging; no stable release has been published.

## Compatibility, risks and parity

No wire format, disk format, public signature, guest image, runtime service or Docker semantics change. The raw loader continues to require enough bytes and a bitwise-copyable representation; all maintained callers load integer/EXT4 metadata types. Existing whole-buffer big-endian reversal is unchanged and is not claimed as new cross-endian support. Docker runtime parity is unaffected by this local decoding primitive, and this unit fix is not full parity evidence.

## Delivery and rollback

Branch `upstream/ext4-unaligned-load` is retained in Stephen's fork for review and generic upstream handoff. No Apple remote writes. The active Container-family build migration owns this branch/worktree; remove them after reviewed merge and consumer pin admission. Reverting the source change restores the previous loader and its known crash; consumers must not claim TSan qualification at that old pin. Next review: 18 September 2026 after native consumer validation.
